package src.vocab;

import javax.swing.*;

// GUI 메인 창 (나중에 구현)
public class MainFrame extends JFrame {
    public MainFrame() {
        setTitle("나만의 영어 단어장");
        setSize(400, 300);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}